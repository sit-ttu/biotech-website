# Biến RIÊNG của app (public site).
# Biến dùng chung (INTERNAL_API_URL, API_ACCESS_KEY) nằm ở ../.env.shared
# — xem .env.shared.example ở root, app tự nạp qua next.config.ts.

# Optional Google Search Console HTML verification token.
GOOGLE_SITE_VERIFICATION=
