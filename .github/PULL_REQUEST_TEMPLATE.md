## What changed

<!-- One or two sentences: what does this PR do, and why? -->

## Area

- [ ] Public site (`app/`)
- [ ] Dashboard (`dashboard/`)
- [ ] Backend API (`backend/`)
- [ ] CI / tooling

## Checklist

- [ ] `pnpm lint` passes for the workspace(s) touched
- [ ] `pnpm build` passes
- [ ] Backend: added/updated tests for new logic (`pnpm test` / `pnpm test:e2e`)
- [ ] Updated relevant docs (`README.md`, `backend/docs/`) if behavior or setup changed
- [ ] No `.env`, `.env.shared`, or `backend/uploads/` files committed

## Related issue

Closes #
